const sueldo = [1360000]
const empleado = ['Andres']
const bonificacion = [200000]

for (let i = []; i = bonificacion; i++) {
    const saldo_total = sueldo+i;
console.log(empleado(saldo_total));
}