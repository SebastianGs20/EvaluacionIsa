let numero= parseInt(prompt("señor usuario digite el numero a factoriar"))
console.log(numero);
for (let i = 0; i < numero; i++) {
    if (numero < 0) {   
        const factorial = numero *[2];
        console.log(factorial);
    }
}